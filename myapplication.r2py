myfile = ABopenfile('story.txt', False)
log("Hello World!")

firstCharacter = myfile.readat(0, 1)
log(firstCharacter)

lengthOfFile = len(myfile.readat(0, None))
log(lengthOfFile)

endPosition = lengthOfFile - 1
lastCharacter = myfile.readat(1, 1)
log(lastCharacter)

# change 'lamb' in first sentence to LAMB
myfile.writeat(LAMB, 18)

# add sentence to the end
myfile.writeat("\nAnd the children loved the lamb as well.", endPosition + None)

myfile.close
